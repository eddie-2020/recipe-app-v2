<%= render "shared/header" %>  
  <section class="body">  
    <h1>Edit your Ingredient</h1>

    <%= form_with model: [@recipe, @recipe_food], html: {'data-turbo' => "false"}, url: { action: "update"}, local: true do |f| %>

    <div class="mb-3">
      <%= f.label :food%>
      <%= f.select :food_id, current_user.foods.collect { |food| [food.name, food.id]}, {include_blank: true}, class: "form-control"%>
    </div>

    <div class="mb-3">
      <%= f.label :quantity %></div>
      <%= f.number_field :quantity, class: "form-control", required: true, min: 1 %>
    </div>

    <div class="mb-3"></div>


    <div class="mb-3">
      <%= f.submit "Update Recipe Food", class: "btn btn-primary" %>
    </div>

    <%end%>
  </section>
<%= render "shared/footer" %>
