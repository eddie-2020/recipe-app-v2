<!DOCTYPE html>
<html>
  <head>
    <title>RecipeAppIi</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <%= render "shared/navbar" %>
    <div class="container my-5">
      <%= render "shared/flash" %>
    </div>
    <%= yield %>
  </body>
</html>
